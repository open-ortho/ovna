name: build-dist-package
on:
  workflow_dispatch:
    
  push:
    branches:
      - 'release/**'
      - '!release/**-dev'
jobs:
  build-tarball:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Make tarball
        run: make tarball